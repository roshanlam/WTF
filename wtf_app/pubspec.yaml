name: wtf_app
description: Free Food Tracker - UMass
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Google Sign-In for authentication
  google_sign_in: ^6.1.5
  
  # State management
  provider: ^6.1.1
  
  # Secure storage for user data
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true